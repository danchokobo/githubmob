# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'githubmobi' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for githubmobi
  pod 'EasyPeasy'
  pod 'Alamofire'
  pod 'Sugar'
  pod 'Reusable'
  pod 'ObjectMapper', '~> 3.3'
end
